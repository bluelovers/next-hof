// Next.js 組態設定 / Next.js configuration
// 極小設定：展示頁只需 App Router 預設行為；typeScript/tsconfig 由既有 tsconfig.json（含 next plugin）處理。
// Minimal config: the showcase page only needs App Router defaults; TypeScript/tsconfig
// are handled by the existing tsconfig.json (which already includes the Next plugin).

import type { NextConfig } from 'next';

const nextConfig: NextConfig = {};

export default nextConfig;
